(import [risp internal primitives])
(export def fn if quote export ... _ defmacro macro-rules)

(export let)
(defmacro let (macro-rules #{} [
  [(let ([name val] ...) expr ...)
   ((fn (name ...) expr ...) val ...)]
]))
